machine:
    php:
        version: 7.1.0
general:
  branches:
    only:
     - test
deployment:
  branch: test
  commands:
    - git checkout successful-test
    - git merge test
    - git push origin successful-test